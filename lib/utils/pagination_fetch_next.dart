import 'package:flutter/foundation.dart';

class PaginationFetchNext {
  final int? currentPage;
  final bool? fetchNext;

  PaginationFetchNext({@required this.currentPage, @required this.fetchNext});
}
